| An error occured
= "\n"
= "\n"
each error in errors
    if error.stack
        | * #{error.stack}
        = "\n"
        = "\n"
    else if Array.isArray(error)
        each sub in error
            if sub.stack
                | * #{sub.stack}
                = "\n"
                = "\n"
            else
                | * #{sub}
                = "\n"
                = "\n"
    else
        | * #{error}
        = "\n"
        = "\n"
