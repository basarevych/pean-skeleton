div.letter
    h3 An error occured
    each error in errors
        if error.stack
            pre= error.stack
        else if Array.isArray(error)
            each sub in error
                if sub.stack
                    pre= sub.stack
                else
                    pre= sub
        else
            pre= error
