/**
 * Translation
 */

'use strict'

module.exports = function (app) {
    return {
        lang: {
            locales: [ 'en' ],              // available locales
            default: 'en',                  // default locale
        },
    };
};
